// Interface que representa um item no catálogo de filmes
export interface CatalogItem {
  name: string;        // Nome do filme
  description: string; // Descrição do filme
  rating: number;      // Avaliação do filme (rating do usuário)
}
